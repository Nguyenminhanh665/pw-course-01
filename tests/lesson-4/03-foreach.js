1.// In ra tất cả các phần tử của một mảng.
const alphabet = ["abc", "def", "ghi"];
alphabet.forEach((value, index) => {
    console.log(value);
})
2.// Tính tổng, tìm giá trị lớn nhất và nhỏ nhất trong một mảng.

const number = [1, 2, 3, 5, 78, 6];
let sum = 0;
let max = arrNums[0];
let min = arrNums[0];
number.forEach ((value) => {
    sum = sum + value;
    if (value > maxmax ) {
        max = value;
 } else if (value < min) {
        min = value;
 }
});
console.log(`Tổng: ${sum}, max: ${max}, min: ${min}`);

3.// Tạo một mảng mới từ một mảng đã cho, trong đó mỗi phần tử được nhân đôi.
const number1 = [1, 2, 3];
let number1Double = [];
number.forEach ((value) => {
    number1Double.push(value * 2);
    console.log(number1Doubl);     
});    
    
        